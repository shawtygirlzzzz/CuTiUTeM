# Normalize line endings to LF in the repo; the app deploys on Linux.
* text=auto eol=lf

# Treat the print-to-PDF spec files as binary (they carry a .md extension).
PRD.md binary
SDD.md binary

# Binary assets
*.png binary
*.ico binary
*.woff2 binary
