# Cloudflare Pages environment variables (Production + Preview).
# Set these in the Pages dashboard, NOT in the repo. This file documents them.

# Gemini API key — server-side only, never exposed to the client (SDD §6.3).
GEMINI_API_KEY=

# The KV namespace bound as RATE_LIMIT is configured in the Pages dashboard,
# not here (SDD §9).
